import { DefaultTheme } from "styled-components"

const darkTheme:DefaultTheme = {
    bgColor: "#2f3640",
    textColor: "#f5f6fa",
    accentColor: "#00a8ff"
}

const lightTheme:DefaultTheme = {
    bgColor: '#dcdde1',
    textColor: 'black',
    accentColor: '#00a8ff'
}

export {
    darkTheme,
    lightTheme
}
